import { ClaimWizard } from '@/components/claim/ClaimWizard';

/** Pick step — entered from /app when the signed-in wallet owns no name. */
export default function ClaimPickPage() {
  return <ClaimWizard />;
}
